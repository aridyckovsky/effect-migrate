{
  "name": "team-dashboard",
  "version": "0.1.0",
  "type": "module",
  "description": "Team dashboard API - halfway migrated to Effect patterns",
  "private": true,
  "scripts": {
    "audit": "effect-migrate audit",
    "metrics": "effect-migrate metrics",
    "audit:amp": "effect-migrate audit --amp-out .amp",
    "metrics:amp": "effect-migrate metrics --amp-out .amp"
  }
}
