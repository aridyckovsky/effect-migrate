{
  "name": "example-file-processor",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Example project showing file processing patterns",
  "scripts": {
    "effect-migrate:audit": "effect-migrate audit",
    "effect-migrate:audit:json": "effect-migrate audit --json"
  },
  "dependencies": {
    "@effect-migrate/core": "workspace:*",
    "@effect-migrate/cli": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
